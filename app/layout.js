export const metadata = {
  title: 'Архив 909',
  description: 'Музыкальный архив',
}

import './globals.css'

export default function RootLayout({ children }) {
  return (
    <html lang="ru">
      <body>{children}</body>
    </html>
  )
}
